input {
  <% if node['chef_server']['logstash']['inputs'].empty? -%>
  tcp {
    type => "tcp-input"
    port => "5959"
    format => "json_event"
  }
  <% else -%>
    <%= LogstashConf.section_to_str(node['chef_server']['logstash']['inputs']) %>
  <% end -%>
}

filter {
}

output {
  stdout { debug => true }
}
