#!/bin/sh

cd /opt/chef-server/embedded/service/geminabox

exec 2>&1
exec chpst -P -U <%= node['chef_server']['user']['username1'] %> -u <%= node['chef_server']['user']['username1'] %> /usr/bin/env HOME="<%= node['chef_server']['geminabox']['dir'] %>" /opt/chef-server/embedded/bin/unicorn -E <%= node['chef_server']['geminabox']['environment'] %> -c <%= File.join(node['chef_server']['geminabox']['dir'], "etc", "unicorn.rb") %> /var/opt/chef-server/geminabox/etc/config.ru
